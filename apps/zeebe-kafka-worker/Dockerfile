# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build zeebe-kafka-worker`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t zeebe-kafka-worker`.
FROM docker.io/node:lts-alpine

WORKDIR /app

RUN addgroup --system zeebe-kafka-worker && \
          adduser --system -G zeebe-kafka-worker zeebe-kafka-worker

COPY dist/apps/zeebe-kafka-worker zeebe-kafka-worker
RUN chown -R zeebe-kafka-worker:zeebe-kafka-worker .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix zeebe-kafka-worker --omit=dev -f install

CMD [ "node", "zeebe-kafka-worker" ]
